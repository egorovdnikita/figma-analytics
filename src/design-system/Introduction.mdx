import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Введение/Figma Analytics Design System" />

# Figma Analytics Design System

Дизайн-система десктопного приложения Figma Analytics: токены и переиспользуемые React-компоненты
из `src/components/ui`. Использует Tailwind CSS поверх семантических CSS-переменных —
никаких сырых hex-значений в компонентах.

## Стек

- **React + TypeScript** — компоненты в `src/components/ui/*.tsx`
- **Tailwind CSS** — утилиты, смэппинг токенов в `tailwind.config.ts`
- **CSS custom properties** — источник истины для цвета, тема переключается классом `.dark`
- **Storybook** — эта документация и песочница для компонентов

## Принципы

1. **Семантические токены, не сырые значения.** Компонент никогда не содержит hex-код —
   только `var(--grass)`, `var(--ink)` и т. д. Смотрите вкладку **Токены → Обзор**.
2. **Тема — это класс, не отдельная тема компонентов.** Один и тот же JSX рендерится
   в двух темах за счёт CSS-переменных; переключите тему тумблером в тулбаре сверху.
3. **Скругления по смыслу.** `rounded-chip` (10px) — для чипов и тегов, `rounded-control`
   (16px) — для полей ввода, кнопок, IconButton, пунктов навигации и трека Segmented,
   `rounded-card` (24px) — для карточек и модалок, `rounded-full` — только для аватаров,
   точек-индикаторов и переключателя Switch.
4. **Активное состояние — это цвет, не заливка.** Пункт навигации, активный сегмент —
   везде подсвечивается цветом текста/иконки (`var(--grass)` или нейтральный `var(--raised)`),
   а не заливкой всего блока брендовым цветом. Заливка целиком зарезервирована для
   первичного действия (`Button` variant `primary`/`danger`).
5. **Минимум вариантов на компонент.** У `Button` пять вариантов (`primary`, `soft`, `outline`,
   `ghost`, `danger`) и три размера — этого достаточно для всего интерфейса приложения,
   новые варианты добавляются только когда реально нужны в продукте.

## Где смотреть код

Исходники компонентов: [`src/components/ui`](https://github.com/egorovdnikita/figma-analytics/tree/main/src/components/ui).
Токены: [`src/index.css`](https://github.com/egorovdnikita/figma-analytics/blob/main/src/index.css) и
[`src/design-system/tokens.ts`](https://github.com/egorovdnikita/figma-analytics/blob/main/src/design-system/tokens.ts).
